# Agent Context for: Gotjobalert

## Memory
=== RECENT DECISIONS ===

- Telegram primary notification: Telegram bot @GotJobAlert_bot

- Base64-encoded token filenames: base64url encoding

- Parser stores provider name (Column M): Column M = NVIDIA/Gemini/Groq/Regex

- Single HTML file (no JS framework): Single HTML + vanilla JS

- gspread over raw Sheets API: gspread

=== LEARNINGS ===

[testing] 11 tests: 8 parser tests (company, date, full email, fallback, dedup) + 3 model tests (validation, sheet row, alert)

[sheets] Sheet formatting: header dark navy (#2a4073), white bold 11pt, alternating blue/white banding, frozen header

[deployment] PythonAnywhere WSGI: sys.path.insert + os.chdir + os.environ before importing app

[flask] Single-threaded scheduler in daemon thread; one slow user poll delays all others

[parser] Date parsing cascade: AI date → regex ISO (YYYY-MM-DD) → regex US (MM/DD/YYYY) → email internalDate

=== PATTERNS ===

- Gmail dedup via Message-ID: Store Message-IDs in sheet column F; skip if already processed; avoids is:unread dependency

- Per-user polling isolation: Each user gets independent Gmail+Sheets service from their own OAuth token; errors isolated

- Scope mismatch detection: In get_creds(), compare requested scopes vs stored token scopes; delete and force re-auth on mismatch

- AI prompt template escaping: Use {{ and }} in f-strings that contain JSON template literals to avoid KeyError

- External cron keepalive: Use cron-job.org to ping app every 15 min to prevent free-tier sleep

- AI + regex dual parser: Try AI first (NVIDIA/Gemini/Groq), fall back to 50+ regex patterns if API fails

- Multi-user token isolation: Store per-user OAuth tokens as credentials/token_<base64(email)>.json for independent Gmail sessions

- SVG pill labels: Always wrap text over arrows in `<g><use href="#pill-COLOR"/><text/></g>` to hide lines behind text

- SVG arrow color rule: Arrow matches DESTINATION box color, not source (Flask→Gmail = red, not blue)

- SVG alignment: All boxes in same layer share same y coordinate; equal gaps between columns (150px margins, 100px gaps)

## Skills

### Designer Skill (`~/.config/opencode/skills/designer/SKILL.md`)
- SVG architecture diagrams: 1000×720 canvas, 7-color accent system, pill labels hide arrows
- Arrow color = destination box color (not source)
- Label midpoints: mx = (x1+x2)/2, my = (y1+y2)/2
- Playwright HTML→PNG carousels: 1200×1200 viewport, `xvfb-run npx playwright screenshot`
- PIL/Pillow for quick prototyping (6/10 quality)
- Dark theme: #0f172a bg, #1e293b cards, #f1f5f9 text, #94a3b8 muted

## Session
ID: a5f7cde8
Updated: Jun 12 — Session 11 (visual overhaul)
