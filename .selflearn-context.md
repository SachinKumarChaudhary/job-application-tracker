# Agent Context for: Gotjobalert

Auto-generated from MemCore.
Last updated: commit e943b36a.

# MemCore Context

## Project: Gotjobalert

## Decisions

- **Fix 'None' channel redirecting to channel selection form**: Changed save_prefs to save 'notification_channel': 'none' instead of deleting the key; added 'notifications disabled' card in Alerts tab for 'none' channel (alt: keep deleting but add special case in template (less clean))

- **Email normalization for Gmail format mismatch**: Added normalize_email() that lowercases and strips dots for gmail.com; added backward-compatible lookup in get_user_prefs() and set_user_pref() to migrate old unnormalized keys; get_creds() renames old token files (alt: only normalize in get_user_email() without backward-compat (broke existing users))

- **Fix channel selection form showing on viewport/UA change**: Added Cache-Control: no-store to all responses via @app.after_request to prevent callback URL caching; changed /callback to redirect to / instead of showing 'Session expired' error (alt: redirect to /auth (auto-triggered Google OAuth, user disliked), email normalization alone (insufficient))

- **Replace Material Symbols briefcase ligature with 'description' icon**: Used 'description' icon name instead of 'briefcase'; user approved. For all other Material Symbols icons, kept ligature names. (alt: emoji (rejected as childish), Unicode PUA \uE9AF (rejected as wrong icon), emoji-only design (user disliked))

- **Designer skill created**: Created ~/.config/opencode/skills/designer/SKILL.md with templates and color system (alt: alt: Inline SVG generation only)

- **SVG architecture diagrams**: Created assets/architecture.svg with 7-color palette, pill labels, drop shadows (alt: alt: Playwright HTML->PNG, PIL/Pillow)

- **Manus AI rejected**: Rejected Manus (alt: Flask continuous)

- **n8n rejected**: Rejected n8n (alt: Flask)

- **Telegram primary notification**: Telegram bot @GotJobAlert_bot (alt: Slack, WhatsApp, Pushover)

- **gspread over raw Sheets API**: gspread (alt: google-api-python-client)

- **Per-user OAuth tokens**: credentials/token_<base64>.json (alt: Single account)

- **cron-job.org keepalive**: cron-job.org (alt: In-app scheduler)

- **AI primary + regex fallback**: Dual path (alt: AI-only, regex-only)

- **NVIDIA over Gemini**: Chose NVIDIA (alt: Gemini, Groq)

- **Flask over FastAPI**: Chose Flask (alt: FastAPI)

## Learnings

  ✓ [parser] Date parsing cascade: AI date -> regex ISO -> regex US -> email internalDate (accessed 1x)

  ✓ [testing] Mock Gmail messages with base64-encoded body for parser tests (accessed 1x)

  ✓ [parser] Three-layer company extraction: domain -> known companies -> generic patterns (accessed 1x)

  ✓ [ai] NVIDIA Llama 3.1 70B has no rate limiting vs Gemini 429 at project level (accessed 1x)

  ✓ [documentation] documentation: docs: create 3 reusable skills — dom-script-execution, cf-worker-api-relay, json-file-persistence (2 files changed: md) (accessed 0x)

  ✓ [documentation] documentation: docs: update for Session 14 — Telegram relay, ntfy, notif_log persistence, alert banner auto-dismiss (4 files changed: md) (accessed 0x)

  ✓ [feature] feature: feat: alert banner auto-dismiss + notif_log persistence

- Alert banner now auto-fades after 3s via polling (setInterval)
- Message appended to Alerts tab as 'Dashboard' entry after fade
- notif_log p (5 files changed: html,py,md) (accessed 0x)

  ✓ [feature] feature: feat: add ntfy.sh, feature-gate WhatsApp, remove dead channels (12 files changed: sh,py,md) (accessed 0x)

  ✓ [bugfix] bugfix: fix: wrap _get_dashboard_context in try/except in all routes; notify_single returns bool (3 files changed: md,py) (accessed 0x)

  ✓ [documentation] documentation: docs: add CLAUDE_DEBUG.md for Claude debugging help (2 files changed: md) (accessed 0x)

  ✓ [documentation] documentation: docs: update PROJECT_COMPLETE.md with test notification fix and alert_message (2 files changed: md) (accessed 0x)

  ✓ [bugfix] bugfix: fix: test notification now handles all channels + visible error feedback

- test_notification route uses notify_single() instead of hardcoded
  channel dispatch (discord, whatsapp_cloud, twilio, pusho (4 files changed: md,py,html) (accessed 0x)

  ✓ [maintenance] maintenance: chore: gitignore generated binaries, scratch files, and agent setup docs (2 files changed: md,gitignore) (accessed 0x)

  ✓ [documentation] documentation: docs: update README and PROJECT_REFERENCE to reflect current architecture

- 7 notification channels (Telegram, Slack, Discord, WhatsApp x3, Pushover)
- 8-stage pipeline with in-place status updates
- (17 files changed: md) (accessed 0x)

  ✓ [documentation] documentation: docs: update README and PROJECT_REFERENCE to reflect current architecture

- 7 notification channels (Telegram, Slack, Discord, WhatsApp x3, Pushover)
- 8-stage pipeline with in-place status updates
- (17 files changed: md) (accessed 0x)

  ✓ [design] Material Symbols ligature rendering can fail on some mobile devices for specific icon names. Using the Unicode PUA character or a different icon name from the same family can bypass the issue. (accessed 0x)

  ✓ [bugfix] Google userinfo endpoint can return the same Gmail address in different formats (Sachin.Kumar vs sachinkumar) depending on user-agent. Always normalize Gmail emails (lowercase, strip dots) before using as dictionary keys. (accessed 0x)

  ✓ [bugfix] Mobile browsers can cache OAuth callback URLs and replay them on user-agent changes (Request Desktop Site). Cache-Control: no-store on all responses prevents this. (accessed 0x)

  ✓ [bugfix] bugfix: fix: auto-skill-gen init arg (0 files changed: ) (accessed 0x)

  ✓ [maintenance] maintenance: chore: test updated memcore hooks (1 files) (accessed 0x)

  ✓ [git] Committed changes @ 797b89f8 (accessed 0x)

  ✓ [git] Committed changes @ c205c579 (accessed 0x)

  ✓ [design] Available visual assets: hero-banner.svg, badges.svg, architecture.svg, how-it-works.svg (accessed 0x)

  ✓ [design] 3-column SVG layout: left center=250, center=500, right=750 on 1000px canvas (accessed 0x)

  ✓ [design] Label pills with colored borders hide arrow lines behind text in SVG diagrams (accessed 0x)

  ✓ [design] SVG arrow color = destination box color, not source (Flask->Gmail = red, not blue) (accessed 0x)

  ✓ [git] Committed changes @ f1c2ef55 (accessed 0x)

  ✓ [sheets] Sheet formatting: header dark navy #2a4073, white bold 11pt, alternating banding (accessed 0x)

  ✓ [termux] Termux proot ptrace restrictions freeze Node.js threads (tracing stop state) (accessed 0x)

  ✓ [flask] Single-threaded scheduler in daemon thread; one slow user poll delays all (accessed 0x)

  ✓ [sheets] Use gspread for Google Sheets; auto-create, append, dedup via Message-ID (accessed 0x)

  ✓ [oauth] OAuth scope mismatch corrupts token files; detect and force re-auth (accessed 0x)

  ✓ [deployment] PythonAnywhere free tier sleeps; cron-job.org ping /cron/secret every 15 min (accessed 0x)

## Patterns

- External cron keepalive (x1)

- AI + regex dual parser (x1)

- Multi-user token isolation (x1)

- Gmail dedup via Message-ID (x1)

- Scope mismatch detection (x1)

- SVG pill labels (x1)

- SVG 3-column layout (x1)

- Gmail email normalization for multi-device auth (x1)

## Skills

- designer: Generate SVGs, HTML-PNG carousels, and PIL diagrams. 7-color system, pill labels, arrow=destination rule. (0 uses, 0% success)

## Self-Reviews

- 33s/0f over 33 entries; 8 patterns → checking skill gen...

- 31s/0f over 31 entries; 8 patterns → checking skill gen...

- 29s/0f over 29 entries; 8 patterns → checking skill gen...