# MemCore Context

## Project: Gotjobalert

## Decisions

- **Manus AI rejected**: Rejected Manus (alt: Flask continuous)

- **n8n rejected**: Rejected n8n (alt: Flask)

- **Telegram primary notification**: Telegram bot @GotJobAlert_bot (alt: Slack, WhatsApp, Pushover)

- **gspread over raw Sheets API**: gspread (alt: google-api-python-client)

- **Per-user OAuth tokens**: credentials/token_<base64>.json (alt: Single account)

- **cron-job.org keepalive**: cron-job.org (alt: In-app scheduler)

- **AI primary + regex fallback**: Dual path (alt: AI-only, regex-only)

- **NVIDIA over Gemini**: Chose NVIDIA (alt: Gemini, Groq)

- **Flask over FastAPI**: Chose Flask (alt: FastAPI)

- **Cloudflare Worker over serveo tunnel**: Permanent URL, free, no SSH tunnel maintenance

- **ntfy.sh over Pushover**: Free, no account needed, open source

- **Polling over MutationObserver**: setInterval for detecting DOM additions via outerHTML (MutationObserver target is destroyed on element replacement)

## Learnings

  ✓ [sheets] Sheet formatting: header dark navy #2a4073, white bold 11pt, alternating banding (accessed 0x)

  ✓ [termux] Termux proot ptrace restrictions freeze Node.js threads (tracing stop state) (accessed 0x)

  ✓ [flask] Single-threaded scheduler in daemon thread; one slow user poll delays all (accessed 0x)

  ✓ [parser] Date parsing cascade: AI date -> regex ISO -> regex US -> email internalDate (accessed 0x)

  ✓ [sheets] Use gspread for Google Sheets; auto-create, append, dedup via Message-ID (accessed 0x)

  ✓ [testing] Mock Gmail messages with base64-encoded body for parser tests (accessed 0x)

  ✓ [parser] Three-layer company extraction: domain -> known companies -> generic patterns (accessed 0x)

  ✓ [ai] NVIDIA Llama 3.1 70B has no rate limiting vs Gemini 429 at project level (accessed 0x)

  ✓ [oauth] OAuth scope mismatch corrupts token files; detect and force re-auth (accessed 0x)

  ✓ [deployment] PythonAnywhere free tier sleeps; cron-job.org ping /cron/secret every 15 min (accessed 0x)

  ✓ [deployment] PythonAnywhere free tier blocks api.telegram.org; Cloudflare Worker relay fixes it (accessed 0x)

  ✓ [javascript] Inline script tags in outerHTML responses do NOT execute; use polling or MutationObserver on a stable parent (accessed 0x)

  ✓ [javascript] MutationObserver target is destroyed when outerHTML replaces the observed element; observe a parent that doesn't get replaced, or use polling (accessed 0x)

  ✓ [flask] notif_log persistence via JSON file load/save on every write; survives app restarts without a database (accessed 0x)

## Patterns

- External cron keepalive (x1)

- AI + regex dual parser (x1)

- Multi-user token isolation (x1)

- Gmail dedup via Message-ID (x1)

- Scope mismatch detection (x1)

- Polling for DOM updates from outerHTML (x1) - `setInterval` check for a known selector, works regardless of DOM replacement mechanism